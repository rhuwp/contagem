// =============================================================================
// CONTROLLER DESATIVADO — a "Fila da Recepção" (lista manual com contador)
// foi substituída pela fila de cotas CONTAGEM_3 (coluna `fila` em pedidos_cota),
// aberta pelas secretárias e despachada pelo mesmo rodízio da fila principal.
//
// A tabela fila_recepcao_pa foi descontinuada (ver migração no deploy).
// PODE APAGAR ESTE ARQUIVO — mantido apenas porque a remoção automática
// não era permitida no ambiente de revisão.
// =============================================================================

module.exports = {};
